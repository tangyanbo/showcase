CREATE TABLE CATEGORY_PRIVILEGE_ROLE_R
(
   ID                   INT NOT NULL AUTO_INCREMENT,
   CATEGORY_ID          INT NOT NULL,
   PRIVILEGE_ID         VARCHAR(50) NOT NULL,
   ROLE_ID              VARCHAR(50) NOT NULL,
   PRIMARY KEY (ID)
);

CREATE UNIQUE INDEX CATEGORY_PRIVILEGE_ROLE_R ON CATEGORY_PRIVILEGE_ROLE_R
(
   CATEGORY_ID,
   PRIVILEGE_ID,
   ROLE_ID
);

CREATE INDEX CATEGORY_CODE_UK ON CATEGORY
(
   CODE
);

insert into role values('TEACHER',NULL,NULL,'教师');
insert into role values('STUDENT',NULL,NULL,'学生');
insert into role values('PARENT',NULL,NULL,'家长');
