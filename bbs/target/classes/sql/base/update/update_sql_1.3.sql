CREATE TABLE STUDENT_TYPE 
(
   STUDENT_TYPE_ID      INT                            NOT NULL AUTO_INCREMENT,
   NAME                 VARCHAR(100)                   NOT NULL UNIQUE,
   PRIMARY KEY (STUDENT_TYPE_ID)
);


ALTER TABLE USER_SY ADD COLUMN STUDENT_TYPE_ID INT;


INSERT INTO  STUDENT_TYPE VALUES (1, '普通学生');

UPDATE USER_SY SET STUDENT_TYPE_ID = 1 WHERE USER_TYPE = 1;


CREATE TABLE FLOOR
(
   FLOOR_ID             INT NOT NULL AUTO_INCREMENT,
   NAME                 VARCHAR(50),
   ADDRESS              VARCHAR(200),
   PRIMARY KEY (FLOOR_ID)
);

CREATE TABLE ROOM
(
   ROOM_ID              INT NOT NULL AUTO_INCREMENT,
   FLOOR_ID             INT,
   NAME                 VARCHAR(50),
   ADDRESS              VARCHAR(200),
   PRIMARY KEY (ROOM_ID)
);

ALTER TABLE ROOM ADD CONSTRAINT FK_FK_FLOOR_ID_ROOM FOREIGN KEY (FLOOR_ID)
      REFERENCES FLOOR (FLOOR_ID) ON DELETE RESTRICT ON UPDATE RESTRICT;